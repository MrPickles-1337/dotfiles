# This is an example Hyprland config file.
# Syntax is the same as in Hypr, but settings might differ.
#
# Refer to the wiki for more information.

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki (basic and advanced configuring)
#

{{ if eq .chezmoi.hostname "archlinux-kirill" -}}

monitor=HDMI-A-3,1920x1080@75,0x0,1
monitor=DP-1,1920x1080@60,1920x0,1
workspace=HDMI-A-3,1

{{ else if eq .chezmoi.hostname "arch-pc-home" -}}

monitor=DP-2,1920x1080@60,-1920x540,1
monitor=DP-1,3840x2160@144,0,1.5,bitdepth,10
monitor=HDMI-A-1,1920x1080@60,2560x540,1
workspace=DP-2,1
workspace=DP-1,2
workspace=HDMI-A-1,5

{{ end -}}

{{ if eq .chezmoi.hostname "archlinux-kirill" -}}
{{ else if eq .chezmoi.hostname "arch-pc-home" -}}
exec-once = discord
windowrule = monitor 2, discord
windowrule = workspace 6, discord

exec-once = telegram-desktop
windowrule = workspace 5, org.telegram.desktop
{{ end -}}

exec-once = /home/kirill/.cargo/bin/swww init
exec-once = dunst
exec-once = waybar
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

{{ if eq .chezmoi.hostname "archlinux-kirill" -}}

env = _JAVA_AWT_WM_NONREPARENTING,1
env = GTK_THEME,Adwaita:dark

{{ else if eq .chezmoi.hostname "arch-pc-home" -}}

env = _JAVA_AWT_WM_NONREPARENTING,1
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = WLR_NO_HARDWARE_CURSORS,1
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = GBM_BACKEND,nvidia-drm
env = LIBVA_DRIVER_NAME,nvidia
env = GTK_THEME,Adwaita:dark
env = LIBVA_DRIVER_NAME,nvidia

{{ end -}}

input {
    kb_layout=us,ru
    kb_variant=
    kb_model=
    kb_options=
    kb_rules=
    repeat_delay=200
    repeat_rate=80

    follow_mouse=1

    left_handed=false

    touchpad {
        natural_scroll=no
    }
}

general {
    {{ if eq .chezmoi.hostname "archlinux-kirill" -}}
    sensitivity=1.0 # for mouse cursor
    {{ else if eq .chezmoi.hostname "arch-pc-home" -}}
    sensitivity=0.6 # for mouse cursor
    {{ end -}}

    gaps_in=5
    gaps_out=20
    border_size=2
    col.active_border=rgb(45138e) rgb(190f92)
    col.inactive_border=0x66333333

    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)

}

decoration {
    rounding=10
    blur {
        new_optimizations=true
        enabled=1
        size=3 # minimum 1
        passes=1 # minimum 1, more passes = more resource intensive.
    }
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
}

animations {
    enabled=1
    animation=windows,1,7,default
    animation=border,1,10,default
    animation=fade,1,10,default
    animation=workspaces,1,6,default
}

dwindle {
    pseudotile=0 # enable pseudotiling on dwindle
}

gestures {
    workspace_swipe=no
}

# example window rules
# for windows named/classed as abc and xyz
#windowrule=move 69 420,abc
#windowrule=size 420 69,abc
#windowrule=tile,xyz
#windowrule=float,abc
#windowrule=pseudo,abc
#windowrule=monitor 0,xyz

windowrule=float,Rofi

windowrule=float,Amberol
windowrule=size 433 686,Amberol

windowrule=size 460 996,scrcpy

windowrule=float,pavucontrol
windowrule=size 903 406,pavucontrol

windowrule=float,OpenTabletDriver.UX.Gtk

$mainMod = Super

# example binds
{{ if eq .chezmoi.hostname "archlinux-kirill" -}}
bind = $mainMod, RETURN, exec, /home/kirill/.cargo/bin/wezterm
{{ else if eq .chezmoi.hostname "arch-pc-home" -}}
bind = $mainMod, RETURN, exec, kitty
{{ end -}}
bind = $mainMod, W, exec, firefox
bind = $mainMod SHIFT, W, exec, swww clear
bind = $mainMod, T, exec, telegram-desktop
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, dolphin
bind = $mainMod, V, togglefloating,
bind = $mainMod, F, fullscreen,
bind = $mainMod, D, exec, pkill -9 rofi; ~/.config/rofi/launchers/type-1/executable_launcher.sh
bind = $mainMod, P, pseudo,
bind = $mainMod SHIFT, L, exec, swaylock --screenshots --clock --indicator --fade-in 1 --effect-blur 5x5
bind = $mainMod SHIFT, s, exec, grimblast copy area
bind = $mainMod, S, exec, scrcpy
bind = $mainMod, R, exec, /home/kirill/.cargo/bin/swww clear; killall wlp_carusel

bind = $mainMod SHIFT, V, exec, pavucontrol

bind = $mainMod, A, exec, hyprctl keyword input:kb_layout ru
bind = $mainMod, Cyrillic_ef, exec, hyprctl keyword input:kb_layout us

bind = $mainMod SHIFT, O, exec, code /home/kirill/src/kubik_flutter
bind = $mainMod, C, exec, wl-color-picker 

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
